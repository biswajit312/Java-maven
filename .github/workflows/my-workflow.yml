{
  "name": "My Workflow",
  "on": {
    "push": {
      "branches": [
        "main"
      ]
    }
  },
  "jobs": {
    "build": {
      "runs_on": "ubuntu-latest",
      "steps": [
        {
          "name": "maven Step",
          "run": "mvn package"
        }
      ]
    }
  }
}